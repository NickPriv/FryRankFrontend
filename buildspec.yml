version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: build

post_build:
  commands:
    - aws s3 sync build/ s3://fryrank-app-spa-bucket/ --delete
