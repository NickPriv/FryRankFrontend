import {PropTypes} from "prop-types";

const propTypes = {
}

const UserSettings = ({userMetadata}) => {
    return (
        <div>
            <p>AAAAA</p>
        </div>
    )
}

export default UserSettings;